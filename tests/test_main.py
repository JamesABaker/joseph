"""Tests for main API endpoints."""

# Note: Endpoint tests are skipped because TestClient triggers app startup
# which tries to connect to PostgreSQL. These would need integration tests
# with a running database or more complex mocking of the database engine.
