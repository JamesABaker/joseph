This is a code manifesto for the repo.

Make sure every small contribution passes the pre-commit checks.

Treat code as a liability. If something can be done with less code, more simply, do it that way.
